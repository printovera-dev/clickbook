# Temporary HTTP-only config used once to obtain the first Let's Encrypt certificate.
server {
    listen 80;
    server_name clickbook.world www.clickbook.world;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 200 'ClickBook bootstrap'; add_header Content-Type text/plain; }
}
