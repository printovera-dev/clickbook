# Docker Compose variables (copy to deploy/.env). Generate a strong password: openssl rand -base64 24
MONGO_ROOT_USER=clickbook
MONGO_ROOT_PASSWORD=change-me-strong-password
LETSENCRYPT_EMAIL=you@clickbook.world
